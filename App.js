import { title, year, nav, h1, probabilities } from './index.js';
import { ranks } from './components/Api.js';



title.innerHTML = `${year} NBA Draft Lottery`;

h1.innerHTML = `Simulate the ${year} NBA Draft Lottery`;
h1.style.backgroundImage = 'linear-gradient(to right, white, red, blue)';

nav.appendChild(h1);

 

  

  

 
